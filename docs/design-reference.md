# 网站视觉参考

2026-09-24，项目方明确要求：**页面风格参照“芯火夺宝”**。
本要求用于后续 M3 网站制作；业务规则仍以开发文档 v0.4 为准。本卡只记录要求，不提前实现页面。

## 参考版本状态

本地精确名称检索未找到直接标注“芯火夺宝”的页面。以下是已定位的相关候选，**M3 前须核对具体版本，不能当作项目方已确认的最终参考**。

候选位置：父工作区 `bem-production-site/web/index.html`，现有标题为“2075 开奖终端 · BNB 主网”，
依次加载 `styles.css`、`festive.css`、`production.css`。
同项目 `bem-production-site/sparkdraw-vault.mjs` 包含品牌字符串 `Tapeout SparkDraw`，但网页仍保留旧标题。
未找到可确认的公开站点 URL。

## 候选中可复核的视觉特征

| 项目 | 源码事实 |
|---|---|
| 背景与正文 | 暖纸底 `#f4eedf`，顶部柔光 `#fffcf4`，正文深棕 `#493a2c`。 |
| 强调与按钮 | 朱红强调 `#bf292c`，主按钮 `#bd282d`，浅米文字 `#fff6e7`。 |
| 卡片 | 浅米底 `#fffcf5`，米金边框 `#e1d4be`，轻阴影，常规圆角 12px；操作卡顶部 2px 红线。 |
| 布局 | 最大宽度 1240px，主内容与右侧操作栏组合；醒目数字、进度条和芯片 SVG 主视觉。 |
| 字体 | 正文声明 `Inter, Segoe UI, Microsoft YaHei, sans-serif`；数字/标签使用 SFMono-Regular、Consolas 等等宽字体；未发现独立加载 Inter 字体文件。 |
| 移动端 | ≤900px 主布局单列；≤760px 左右留白 16px、钱包操作换行；≤500px 留白 11px、侧栏单列；主要操作最小高度 48px。 |
| 动效 | 支持 `prefers-reduced-motion`。 |

上述值主要来自候选的 `festive.css` 第 2/30/36/241 行、`styles.css` 第 48/63/66/67 行和
`production.css` 第 1/3 行，后续以核对过的实际页面为准。

M3 参考其视觉语言，认购、领取、共同决策及出售等交互按本项目规格设计；候选中的抽奖交互不属于本项目需求。
父工作区 `mining-pool-site` 的薄荷绿拼矿原型没有被项目方选作本次视觉依据。
