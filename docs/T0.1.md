# T0.1 初始化仓库交付记录

日期：2026-09-24。范围：开工计划第 4 节 T0.1。

**T0.1 本地检查、远端推送及 GitHub CI 实跑已完成，等待项目方验收。**
目标仓库已按用户指示切换为 [jianfengliao774-sketch/pinkuang](https://github.com/jianfengliao774-sketch/pinkuang)。
本机 Git 写入成功，初始化内容位于 `codex/t0-1-bootstrap`，已建立 [草稿 PR #1](https://github.com/jianfengliao774-sketch/pinkuang/pull/1)。
`main` 是初始化基线，业务工程在开发分支供审查，尚未合并。没有修改父项目已有文件。

远端首次验证提交：`fb3c7adc6be76f5271e3ce047ab747f397d6bf27`。
[GitHub Actions #1](https://github.com/jianfengliao774-sketch/pinkuang/actions/runs/35977060987) 已完成，结论 `success`；
contracts 工作任务所有步骤成功，fork 工作任务按本卡范围跳过。
完整远端原始日志见 [github-actions-35977060987.log](logs/T0.1/github-actions-35977060987.log)。
空测试与不适用的审计步骤仅证明工程检查链路可运行，不代表 M0 协议验证或 M1 合约安全验收通过。

## 1. 文件改动

| 文件 | 说明 |
|---|---|
| `README.md` | 说明当前阶段、目录、固定工具版本、安装检查和后续验收边界。 |
| `.gitignore` | 忽略依赖、临时工具、编译产物、环境文件及常见密钥文件。 |
| `.gitattributes` | 统一文本 LF，DOCX 按二进制保存。 |
| `.nvmrc` | 固定 Node.js 24.19.0。 |
| `.env.example` | 说明只读 RPC、固定区块和 Windows Bash 路径；不包含私钥。 |
| `package.json` | 固定合约依赖和本地检查入口。 |
| `package-lock.json` | 锁定全部 NPM 依赖及下载校验值。 |
| `contracts/foundry.toml` | Solidity 0.8.24、Shanghai、optimizer 200、via_ir=false、FFI/AST/build-info/storageLayout。 |
| `contracts/remappings.txt` | 映射两套 OpenZeppelin、升级插件及 forge-std 测试工具。 |
| `contracts/script/Addresses.sol` | 固定 chainId 56 和用户提供的 11 个地址，均标记「待人工复核」。 |
| `contracts/test/utils/DependencyImports.sol` | 实际编译所需依赖，避免空项目构建掩盖依赖不兼容；不包含测试或业务合约。 |
| `contracts/src/.gitkeep` | 保留业务合约目录。 |
| `contracts/src/interfaces/.gitkeep` | 保留协议接口目录。 |
| `contracts/src/libraries/.gitkeep` | 保留未来按体积拆分的库目录。 |
| `contracts/test/unit/.gitkeep` | 保留单元测试目录。 |
| `contracts/test/fork/.gitkeep` | 保留协议 fork 测试目录。 |
| `contracts/test/invariant/.gitkeep` | 保留不变量测试目录。 |
| `indexer/.gitkeep` | 保留 Ponder 索引服务目录。 |
| `api/.gitkeep` | 保留 API 目录。 |
| `keeper/.gitkeep` | 保留定时任务目录。 |
| `web/.gitkeep` | 保留网站目录。 |
| `bot/.gitkeep` | 保留 Telegram 机器人目录。 |
| `scripts/check-local.mjs` | 处理 Windows 中文路径编译问题，校验临时副本并记录原始输出和退出码。 |
| `scripts/run-fork.mjs` | 强制提供固定区块、RPC 和真实 ProtocolProbe，禁止静默使用 latest 或空测试。 |
| `scripts/validate-upgrades.mjs` | 接入升级插件使用的验证引擎；当前无业务实现时如实标记不适用。 |
| `.github/workflows/contracts.yml` | 配置格式、体积、非 fork 测试、Slither、升级检查及手动 fork 工作流。 |
| `docs/sources/development-spec-v0.4.docx` | 原样保留用户指定的 v0.4 开发文档。 |
| `docs/sources/kickoff-plan-v0.4.md` | 原样保留开工计划。 |
| `docs/sources/sha256.json` | 两份原始需求文件的 SHA-256。 |
| `docs/T0.1.md` | 本次交付和未完成事项。 |
| `docs/design-reference.md` | 记录项目方指定的“芯火夺宝”视觉方向和待核对的本地候选，留待 M3 落实。 |
| `docs/logs/T0.1/*` | 保存工具版本、依赖安装、格式、编译尺寸、空测试、检查范围、源码校验及依赖审计输出。 |

## 2. 测试命令和原始输出

Windows 本机运行入口：`node scripts/check-local.mjs`，使用 `FOUNDRY_PROFILE=ci`。
由于 solc 0.8.24 Windows 二进制无法正确解析当前中文路径，命令在英文临时目录的同内容副本执行；
目录、可执行文件和全部退出码记录于 [summary.json](logs/T0.1/summary.json)，
源码校验值记录于 [source-sha256.json](logs/T0.1/source-sha256.json)。

`npm ci`：退出码 0，完整输出见 [npm-ci.log](logs/T0.1/npm-ci.log)。
安装了 167 个依赖包；审计报告有 6 个 Low 级别受影响依赖节点，均追溯到
elliptic 的同一条开发工具依赖链告警，详见 [npm-audit.json](logs/T0.1/npm-audit.json)。
没有报告 Medium/High/Critical；本记录不等于业务合约安全审计。

`forge fmt --check`：退出码 0，无输出，见 [forge-fmt.log](logs/T0.1/forge-fmt.log)。

`forge test --no-match-path 'test/fork/**' -vv`：退出码 0，原始输出如下，完整日志见
[forge-test.log](logs/T0.1/forge-test.log)。

```text
No files changed, compilation skipped
No tests found in project! Forge looks for functions that start with `test`
```

这是空测试的正常退出，**没有业务测试、没有协议测试通过**。

Slither：当前 `contracts/src` 没有业务 Solidity 源文件，因此明确标记不适用；没有声称完成合约静态审计。
原始范围说明见 [slither.log](logs/T0.1/slither.log)。

```text
NOT APPLICABLE (T0.1): no business Solidity sources for Slither.
```

`node scripts/validate-upgrades.mjs`：退出码 0，当前没有业务实现或旧版布局可比对，
原始输出见 [upgrade-validation.log](logs/T0.1/upgrade-validation.log)。

```text
NOT APPLICABLE (T0.1): no business implementations or prior storage layout exist.
```

M1 起必须补齐具体实现合约的升级验证和参考版本；本卡的依赖编译不能替代升级布局兼容测试。

补充配置检查：JavaScript 语法检查通过；YAML 可解析且包含预期触发器和两个工作任务，
依赖清单与 lockfile 一致，两份需求文件 SHA-256 一致，见 [config-checks.log](logs/T0.1/config-checks.log)。
fork 入口在缺少环境变量或缺少 ProtocolProbe 时均按预期退出 1，见
[fork-guards.log](logs/T0.1/fork-guards.log)。这些检查未发送 RPC 或链上交易，不是协议验证。

继续检查时修正了 Slither 参数冲突：`--fail-high` 与 `--fail-medium` 在 0.11.5 中互斥，
CI 和本地检查入口统一只保留 `--fail-medium`，仍同时拦截 Medium / High；已核对该版本的参数解析与退出逻辑。

## 3. forge build --sizes

退出码 0；Solidity 0.8.24 编译 72 个源文件成功。
完整原始输出见 [forge-build-sizes.log](logs/T0.1/forge-build-sizes.log)。
其中包括被编译的依赖库和代理，不应把依赖合约体积当作尚未实现的 PoolVault 体积。

| 项目 | Runtime bytes | Initcode bytes |
|---|---:|---:|
| Addresses | 85 | 135 |
| TimelockController（依赖） | 6,536 | 7,500 |
| UpgradeableBeacon（依赖） | 644 | 1,057 |
| BeaconProxy（依赖） | 283 | 1,431 |

## 4. 偏差及未完成清单

- 业务规则变更：无。没有开始 T0.2、M1 或任何部署。
- 地址按用户本次补充要求写死并注明「待人工复核」；未声明完成人工 BscScan 复核。
- 依赖采用固定版本 NPM 安装而非 Git submodule；开工计划没有规定安装器，仍是指定的 Foundry/OZ 技术栈。
- 本机通过英文临时副本绕过旧版 Windows solc 的中文路径问题；没有修改编译器版本、EVM 设置或优化配置。
- GitHub 远端推送、草稿 PR 和 Actions 实际运行已完成；本报告附具体运行链接、提交及原始日志。
- 协议结论 Q1–Q9、固定 FORK_BLOCK、市场手续费及铸币权限均留待 T0.2，未提前作结论。

## 5. 需要项目方处理或决定

当前仓库为 `jianfengliao774-sketch/pinkuang`，写入与 CI 障碍已解决，项目方无需继续处理旧仓库权限。
请按本卡交付范围验收 T0.1；下一张任务卡为 T0.2 协议验证。

本卡验收后再进入 T0.2。计划第 7 节的出售路径、成员地址口径、购机双路径及铸币风险提示等决策，
本次工程初始化不替项目方作决定，后续相关任务需沿用明确确认结果。
